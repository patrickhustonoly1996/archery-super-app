defaultTracker = "json"
maxIterations = 0
iterationDelay = 1000
outputDir = ".ralph-output"
subagentTracingDetail = "moderate"

[[agents]]
name = "claude"
plugin = "claude"
defaultFlags = [ "--dangerously-skip-permissions" ]

[agents.options]
model = "sonnet"
